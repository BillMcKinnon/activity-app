.container#home_container
  = image_tag("logo.jpg", alt: "Onward logo. The word 'onward' in a black flowy font with a black Enso circle surrounding it.")
  %p Onward tracks the time you put into things like personal development (contributors) vs R&R (subtractors). 
  %p The idea is to maintain a positive balance to stay ahead, increase personal accountability, and avoid things like post-Netflix binge guilt.  

  - if !logged_in?
    = form_for(:session, url: login_path, html: { id: "login_form", class: "sm-screen-form" }) do |f|
      = f.label :email
      = f.email_field :email

      = f.label :password
      = f.password_field :password

      = f.label :remember_me, { class: "remember-checkbox" } do
        %span.label-body Remember?
        = f.check_box :remember_me

      = f.submit "Log in"

    %p.separator
      %span or
    = link_to "Forgot Password", new_password_reset_path, id: "forgot_password_link"
    %br
    = link_to "Sign Up", signup_path, id: "signup_link"
    %br
    =link_to "Privacy", privacy_path, id: "privacy_link"

